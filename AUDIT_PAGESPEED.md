# Аудит PageSpeed — liteh26.ru

## 1. Hero / LCP-изображение первого экрана

**Результат:** Отдельного hero-изображения на главной нет. Кандидат в LCP — логотип в шапке.

| Где | Что проверено | Статус |
|-----|----------------|--------|
| `src/components/header.tsx` | Логотип в шапке | ✅ Уже `next/image`, `priority`, `sizes="(max-width: 640px) 120px, 140px"`, `width={140}` `height={44}` |
| `src/components/header.tsx` | Лого в drawer (мобильное меню) | ✅ `next/image`, `sizes="110px"`, width/height заданы |
| Главная страница | Крупное изображение выше первого экрана | ✅ Нет — только текст, поиск, кнопки |

**Вывод:** Менять ничего не нужно. Hero-оптимизация уже сделана.

---

## 2. Lazy loading секций ниже первого экрана

| Секция | Текущее состояние | Действие |
|--------|-------------------|----------|
| PromoCarousel (Акции) | ✅ Уже `dynamic(import(...))` с loading-плейсхолдером | Не трогать |
| ReviewsCarousel (Отзывы) | ✅ Уже `dynamic(import(...))` с loading-плейсхолдером | Не трогать |
| FAQ (Вопросы и ответы) | Рендерятся inline в `page.tsx` | ✅ Безопасно: вынести в компонент и подгружать через `dynamic(..., { ssr: true })` — контент остаётся в HTML для SEO, уменьшается размер начального JS-чанка |
| Секция филиалов, «Как подготовиться», нижний CTA | Статичный HTML | Опционально можно лениво подгружать позже; сейчас не меняем |

**Вывод:** Единственная безопасная доработка — ленивая подгрузка FAQ (один новый компонент + `dynamic` в `page.tsx`).

---

## 3. Unused JavaScript

| Пакет | Использование | Действие |
|-------|----------------|----------|
| `axios` | Используется в `scripts/parse.js` (require("axios")) | ❌ Не удалять |
| `embla-carousel-react` | PromoCarousel, ReviewsCarousel (динамические импорты) | Используется |
| `lucide-react` | bottom-nav.tsx (иконки) | Используется |
| `cheerio`, `nodemailer`, `puppeteer`, `xlsx` | Скрипты / API / билды | Не трогать |

**Вывод:** Неиспользуемых зависимостей в коде приложения нет. Удалять ничего не будем.

---

## 4. Кеширование и next.config

| Настройка | Текущее состояние | Действие |
|-----------|-------------------|----------|
| `images.formats` | ✅ `["image/webp", "image/avif"]` | Уже есть |
| `images.deviceSizes` | ✅ Заданы (в т.ч. 384 для мобильных) | Уже есть |
| `compress` | В Next.js по умолчанию `true` в production | Явно не добавляем |

**Вывод:** Конфиг в порядке, изменений не требуется.

---

## 5. Итог безопасных изменений

- **Hero / next/image:** без изменений.
- **Lazy loading:** добавить динамическую подгрузку секции FAQ (новый компонент + `dynamic` в `page.tsx`).
- **Unused JS:** без изменений.
- **next.config:** без изменений.

После аудита выполняется только пункт «Lazy loading FAQ».
